# nvchecker.toml — upstream version checker config for home:itachi_re
#
# Docs: https://nvchecker.readthedocs.io/en/latest/usage.html

[__config__]
oldver = "versions.json"
newver = "versions_new.json"
max_concurrency = 3
# Optional: GitHub token for higher API rate limits
# keyfile = ".nvchecker_keyfile.toml"


# ══════════════════════════════════════════════════════════════════════════════
# Android / Waydroid ecosystem
# ══════════════════════════════════════════════════════════════════════════════

[anbox-modules]
source = "github"
github = "llyyr/anbox-modules"
use_max_tag = true

[android-apktool]
source = "github"
github = "iBotPeaches/Apktool"
use_max_tag = true

[android-build-tools]
source = "android_sdk"
android_sdk = "build-tools"
repo = "package"
channel = "stable"

#[android-platform-tools]
#source = "android_sdk"
#android_sdk = "platform-tools"
#repo = "package"

#[android-studio]
#source = "github"
#github = "itachi-re/android-studio-obs"
#use_max_tag = true

[libgbinder]
source = "github"
github = "mer-hybris/libgbinder"
use_commit = true

[libglibutil]
source = "github"
github = "sailfishos/libglibutil"
use_commit = true

[python-gbinder]
source = "github"
github = "waydroid/gbinder-python"
use_max_tag = true

[waydroid]
source = "github"
github = "waydroid/waydroid"
use_commit = true

[waydroid-image]
source = "github"
github = "itachi-re/waydroid-image-obs"
use_max_tag = true

# waydroid-image-gapps / waydroid-image-vanilla: build outputs, not upstream
# projects — tracking them makes no sense. REMOVED.

[waydroid-kmp-default]
source = "github"
github = "llyyr/anbox-modules"
use_max_tag = true

[waydroid-magisk]
source = "github"
github = "nitanmarcel/waydroid-magisk"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Arduino / Embedded
# ══════════════════════════════════════════════════════════════════════════════

[arduino-ide]
source = "github"
github = "arduino/arduino-ide"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Arch Linux tools
# ══════════════════════════════════════════════════════════════════════════════

#[arch-install-scripts]
#source = "github"
#github = "archlinux/arch-install-scripts"
#use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Fun / Terminal toys
# ══════════════════════════════════════════════════════════════════════════════

[asciiquarium]
source = "github"
github = "itachi-re/asciiquarium"
use_max_tag = true

[pipes-rs]
source = "github"
github = "lhvy/pipes-rs"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Audio / Media
# ══════════════════════════════════════════════════════════════════════════════

[audio-share]
source = "github"
github = "mkckr0/audio-share"
use_max_tag = true

[hypnotix]
source = "github"
github = "linuxmint/hypnotix"
use_max_tag = true

[fred-tv]
source = "git"
git = "https://github.com/Fredolx/open-tv.git"
use_max_tag = true

[iptvnator]
source = "git"
git = "https://github.com/4gray/iptvnator.git"
use_max_tag = true

[megacubo]
source = "git"
git = "https://github.com/EdenwareApps/Megacubo.git"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Email
# ══════════════════════════════════════════════════════════════════════════════

[betterbird]
source = "github"
github = "Betterbird/thunderbird-patches"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Work
# ══════════════════════════════════════════════════════════════════════════════
[onlyoffice-desktopeditors]
source = "github"
github = "ONLYOFFICE/DesktopEditors"
use_max_tag = true

[joplin-desktop]
source = "github"
github = "laurent22/joplin"
use_latest_release = true
include_regex = "^v[\\d.]+$"

# ══════════════════════════════════════════════════════════════════════════════
# System utilities
# ══════════════════════════════════════════════════════════════════════════════

[bleachbit]
source = "github"
github = "bleachbit/bleachbit"
use_max_tag = true

[fastfetch]
source = "github"
github = "fastfetch-cli/fastfetch"
use_max_tag = true

[flameshot]
source = "github"
github = "flameshot-org/flameshot"
use_max_tag = true

[iotop]
source = "github"
github = "Tomas-M/iotop"
use_max_tag = true

[input-remapper]
source = "github"
github = "sezanzeb/input-remapper"
use_latest_release = true

[zypperoni]
source = "github"
github = "pavinjosdev/zypperoni"
use_max_tag = true

[zypperx]
source = "github"
github = "itachi-re/zypperx"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Browsers
# ══════════════════════════════════════════════════════════════════════════════

#[brave-origin-beta]
#source = "cmd"
#cmd = "curl -fsSL -H \"Authorization: Bearer $NVCHECKER_GITHUB_TOKEN\" \"https://api.github.com/repos/brave/brave-browser/releases?per_page=10\" | jq -r '[.[] | select(.name | startswith(\"Beta\"))] | first | .tag_name | ltrimstr(\"v\")'"
#include_pattern = "^[0-9]+\\.[0-9]+\\.[0-9]+$"

[brave-browser-beta]
source = "cmd"
cmd = "curl -fsSL -H \"Authorization: Bearer $NVCHECKER_GITHUB_TOKEN\" \"https://api.github.com/repos/brave/brave-browser/releases?per_page=20\" | jq -r '[.[] | select(.name != null and (.name | test(\"^Beta v[0-9]+[.][0-9]*[02468]\")))] | first | .tag_name | ltrimstr(\"v\")'"
include_pattern = "^[0-9]+\\.[0-9]+\\.[0-9]+$"

#[brave-origin-beta]
#source = "cmd"
#cmd = "curl -fsSL -H \"Authorization: Bearer $NVCHECKER_GITHUB_TOKEN\" \"https://api.github.com/repos/brave/brave-browser/releases?per_page=20\" | jq -r '[.[] | select(.name != null and (.name | test(\"^Beta v[0-9]+[.][0-9]*[13579][.]\")))] | first | .tag_name | ltrimstr(\"v\")'"
#include_pattern = "^[0-9]+\\.[0-9]+\\.[0-9]+$"

[brave-origin-beta]
source = "regex"
url = "https://versions.brave.com/latest/brave-versions.json"
regex = '"name":"([0-9]+\.[0-9]+\.[0-9]+)","channel":"beta"'

#[brave-browser]
#source = "github"
#github = "brave/brave-browser"
#use_max_tag = true

#[ff-dev-edition]
#source = "github"
#github = "itachi-re/ff-dev-obs"
#use_max_tag = true

[zen-browser]
source = "git"
git = "https://github.com/zen-browser/desktop.git"
use_max_release = true

[floorp]
source = "git"
git = "https://github.com/Floorp-Projects/Floorp.git"
use_max_release = true

[firedragon]
source = "git"
git = "https://gitlab.com/garuda-linux/firedragon/firedragon13.git"
use_max_release = true

# ══════════════════════════════════════════════════════════════════════════════
# Network / VPN
# ══════════════════════════════════════════════════════════════════════════════

[cloudflare-warp]
source = "cmd"
cmd = "curl -s 'https://pkg.cloudflareclient.com/dists/noble/main/binary-amd64/Packages.gz' | zcat | awk '/^Package: cloudflare-warp/{found=1} found && /^Version:/{print $2; exit}'"

[wgcf]
source = "github"
github = "ViRb3/wgcf"
use_max_tag = true

[wg-info]
source = "git"
git = "https://codeberg.org/itachi-re/wg-info.git"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Development tools
# ══════════════════════════════════════════════════════════════════════════════

[codium]
source = "github"
github = "VSCodium/vscodium"
use_max_tag = true

[gradle]
# The _service was pointing at the GitHub releases API — use GitHub directly
source = "github"
github = "gradle/gradle"
use_latest_release = true

[codeblocks]
# Code::Blocks — tracked via Anitya (Sourceforge upstream)
source = "anitya"
anitya = "fedora/codeblocks"

[obsidian]
source = "github"
github = "obsidianmd/obsidian-releases"
use_max_tag = true

[rkdeveloptool]
source = "github"
github = "rockchip-linux/rkdeveloptool"
use_commit = true

[rkflashtool]
source = "github"
github = "linux-rockchip/rkflashtool"
use_commit = true

# ══════════════════════════════════════════════════════════════════════════════
# Fonts
# ══════════════════════════════════════════════════════════════════════════════

[monaspace-fonts]
source = "github"
github = "githubnext/monaspace"
use_max_tag = true

[nerd-fonts]
source = "github"
github = "ryanoasis/nerd-fonts"
use_max_tag = true

[circle-flags]
source = "github"
github = "linuxmint/circle-flags"
use_max_tag = true

[iso-country-flags]
source = "github"
github = "linuxmint/flags"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Gaming / Wine / Emulation
# ══════════════════════════════════════════════════════════════════════════════

[faugus-launcher]
source = "github"
github = "Faugus/faugus-launcher"
use_max_tag = true

[protonup-qt]
source = "github"
github = "DavidoTek/ProtonUp-Qt"
use_max_tag = true

[ProtonPlus]
source = "git"
git = "https://github.com/Vysp3r/ProtonPlus.git"
use_max_release = true

[vermouth]
source = "git"
git = "https://github.com/dekomote/vermouth.git"
use_commit = true

[rbdoom3bfg]
source = "github"
github = "RobertBeckebans/RBDOOM-3-BFG"
use_max_tag = true

[windows95]
source = "github"
github = "felixrieseberg/windows95"
use_max_tag = true

[winboat]
source = "github"
github = "TibixDev/winboat"
use_max_tag = true

[gamescope]
source = "git"
git = "https://github.com/ValveSoftware/gamescope.git"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Manga / Comics
# ══════════════════════════════════════════════════════════════════════════════

[mangayomi]
source = "github"
github = "kodjodevf/mangayomi"
use_max_tag = true

[komikku]
source = "git"
git = "https://codeberg.org/valos/Komikku.git"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# KDE / Plasma extras
# ══════════════════════════════════════════════════════════════════════════════

[kde-material-you-colors]
source = "github"
github = "luisbocanegra/kde-material-you-colors"
use_max_tag = true

[kurve]
source = "github"
github = "luisbocanegra/kurve"
use_max_tag = true

[plasma-panel-colorizer]
source = "github"
github = "luisbocanegra/plasma-panel-colorizer"
use_max_tag = true

[plasma-panel-spacer-extended]
source = "github"
github = "luisbocanegra/plasma-panel-spacer-extended"
use_max_tag = true

[plasma-smart-video-wallpaper-reborn]
source = "github"
github = "luisbocanegra/plasma-smart-video-wallpaper-reborn"
use_max_tag = true

[plasma-wallpaper-effects]
source = "github"
github = "luisbocanegra/plasma-wallpaper-effects"
use_max_tag = true

[plasma6-ksplash-bgrt]
source = "github"
github = "kbroulik/ksplash-bgrt"
use_max_tag = true

[plasma6-theme-apus]
source = "git"
git = "https://gitlab.com/jomada/apus.git"
use_commit = true

[plasma6-theme-klassy]
source = "github"
github = "paulmcauley/klassy"
use_max_tag = true

[plasma6-theme-otto]
source = "git"
git = "https://gitlab.com/jomada/otto.git"
use_commit = true

# ══════════════════════════════════════════════════════════════════════════════
# KDE Applications
# ══════════════════════════════════════════════════════════════════════════════

[krita]
source = "gitlab"
host = "invent.kde.org"
gitlab = "graphics/krita"
use_commit = true

[kseexpr]
source = "gitlab"
host = "invent.kde.org"
gitlab = "graphics/kseexpr"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Kernel
# ══════════════════════════════════════════════════════════════════════════════

[kernel-xanmod-v3]
source = "git"
git = "https://gitlab.com/xanmod/linux.git"
use_max_tag = true
# Xanmod tags look like "6.9.3-xanmod1" — add include_pattern if needed:
# include_pattern = "^[0-9]+\\.[0-9]+\\.[0-9]+-xanmod[0-9]+$"

[xanmod-edge-custom]
source = "git"
git = "https://gitlab.com/xanmod/linux.git"
use_max_tag = true

# kernel-copyto: custom/local kernel module — no public upstream. Skip.

# ══════════════════════════════════════════════════════════════════════════════
# Microsoft / DirectX
# ══════════════════════════════════════════════════════════════════════════════

[DirectXShaderCompiler]
source = "github"
github = "microsoft/DirectXShaderCompiler"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Lua / Perl / Python packages
# ══════════════════════════════════════════════════════════════════════════════

[lua51-lpeg]
source = "github"
github = "roberto-ieru/LPeg"
use_max_tag = true

[python-pyclip]
source = "github"
github = "spyoungtech/pyclip"
use_max_tag = true

[python3-materialyoucolor]
source = "github"
github = "T-Dynamos/materialyoucolor-python"
use_max_tag = true

[python3-pywal16]
source = "github"
github = "eylles/pywal16"
use_max_tag = true

[python-markitdown]
source = "github"
github = "microsoft/markitdown"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Qt / Multimedia
# ══════════════════════════════════════════════════════════════════════════════

[qt6-multimedia-ffmpeg]
source = "github"
github = "qt/qtmultimedia"
use_max_tag = true

#[qtscrcpy]
#source = "github"
#github = "barry-ran/QtScrcpy"
#use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Torrents
# ══════════════════════════════════════════════════════════════════════════════

[qBittorrent-Enhanced-Edition]
source = "github"
github = "c0re100/qBittorrent-Enhanced-Edition"
use_latest_release = true

# ══════════════════════════════════════════════════════════════════════════════
# Boot / EFI
# ══════════════════════════════════════════════════════════════════════════════

[rEFInd]
source = "git"
git = "https://gitlab.com/btklein/refind.git"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Remote desktop / Screen sharing
# ══════════════════════════════════════════════════════════════════════════════

[droidcam]
source = "github"
github = "dev47apps/droidcam-linux-client"
use_max_tag = true

[rustdesk]
source = "github"
github = "rustdesk/rustdesk"
use_max_tag = true

[scrcpy]
source = "github"
github = "Genymobile/scrcpy"
use_latest_release = true

[scrcpy-autoadb]
source = "github"
github = "nickcoutsos/scrcpy-autoadb"
use_max_tag = true

[freerdp]
source = "git"
git = "https://github.com/FreeRDP/FreeRDP.git"
use_commit = true

[remmina]
source = "git"
git = "https://gitlab.com/Remmina/Remmina.git"
use_commit = true

# ══════════════════════════════════════════════════════════════════════════════
# Messaging / Social
# ══════════════════════════════════════════════════════════════════════════════

[telegram]
source = "github"
github = "telegramdesktop/tdesktop"
use_max_tag = true

[webcord]
source = "github"
github = "SpacingBat3/WebCord"
use_max_tag = true

[win11-clipboard-history]
source = "github"
github = "gustavosett/Windows-11-Clipboard-History-For-Linux"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Storage / ISO tools
# ══════════════════════════════════════════════════════════════════════════════

[ventoy]
source = "github"
github = "ventoy/Ventoy"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Package management / Utilities
# ══════════════════════════════════════════════════════════════════════════════

[TuxManager]
source = "github"
github = "benapetr/TuxManager"
use_commit = true

# ══════════════════════════════════════════════════════════════════════════════
# AppImage management
# ══════════════════════════════════════════════════════════════════════════════

[gearlever]
source = "github"
github = "mijorus/gearlever"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# RGB / Hardware
# ══════════════════════════════════════════════════════════════════════════════

[openrgb]
source = "git"
git = "https://gitlab.com/CalcProgrammer1/OpenRGB.git"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Shell plugins
# ══════════════════════════════════════════════════════════════════════════════

[zsh-autosuggestions]
source = "github"
github = "zsh-users/zsh-autosuggestions"
use_max_tag = true

[zsh-syntax-highlighting]
source = "github"
github = "zsh-users/zsh-syntax-highlighting"
use_max_tag = true

# ══════════════════════════════════════════════════════════════════════════════
# Video calls
# ══════════════════════════════════════════════════════════════════════════════

[zoom]
source = "cmd"
cmd = '''
curl -fsSL "https://zoom.us/download?os=linux" |
grep -oP 'zoom_(amd64|openSUSE_x86_64)-\K[0-9.]+(?=\.(deb|rpm))' |
head -n1
'''
include_pattern = "^[0-9.]+$"

# ══════════════════════════════════════════════════════════════════════════════
# Download managers
# ══════════════════════════════════════════════════════════════════════════════

[abdm]
source = "github"
github = "amir1376/ab-download-manager"
use_max_tag = true

[yt-dlp]
source = "git"
git = "https://github.com/yt-dlp/yt-dlp"
use_commit = true

[media-downloader]
source = "git"
git = "https://github.com/mhogomchungu/media-downloader.git"
use_commit = true

[jdownloader2]
source = "regex"
url = "https://jdownloader.org/jdownloader2"
regex = 'JDownloader2Setup_([0-9.]+)\.exe'
